kind: Service                             
apiVersion: v1
metadata:
  name: minha_aplicacao
  labels:
    app: minha_aplicacao
spec:
  ports:
    - port: 8085                               
      targetPort: 80                    
      nodePort: 31200
  selector:
    app: minha_aplicacao                    
  type: LoadBalancer   
